# NATS client (TCP) listener
port: 4222

# WebSocket listener (for browser clients)
websocket {
  listen: "0.0.0.0:9222"
  no_tls: true              # Set to false and add TLS config for production
  # Optional: restrict allowed origins (CORS)
  # allowed_origins: ["http://localhost:3000"]
}


